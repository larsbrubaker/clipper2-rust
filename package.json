{
  "name": "clipper2-rust-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wasm-pack build demo/wasm --target web --out-dir ../public/pkg && bun run --cwd demo dev",
    "dev:watch": "bun run --cwd demo dev"
  }
}
