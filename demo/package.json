{
  "name": "clipper2-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --watch run server.ts",
    "build:wasm": "powershell -File build-wasm.ps1",
    "build": "bun run build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
